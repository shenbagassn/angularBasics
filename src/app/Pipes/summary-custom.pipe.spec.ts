import { SummaryCustomPipe } from './summary-custom.pipe';

describe('SummaryCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
